# 题库数据、加密密钥、上传中间产物 —— 全部是本机私有内容，绝不提交
data/

# vendor/project_alpha 的运行期产物与真实密钥
vendor/project_alpha/output/
vendor/project_alpha/.env

# 导出产物（生成的 PDF/Tex）
output/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
node_modules/
.playwright-cli/
build/
dist/
/$out/
/_pre_sync_backup/

# 任意 .env（真实密钥），只保留 .env.example
.env
*.env
!.env.example
!vendor/project_alpha/.env.example

# IDE / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db

# 商业发行用的第三方离线运行时体积很大，且须单独做许可证审计，不进源码仓库。
runtime/pandoc/
runtime/tex/

# Obsidian 库配置（机器特定）
.obsidian/
