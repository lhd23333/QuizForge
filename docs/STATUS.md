# QuizForge 软件版当前状态

> 当前快照：2026-09-04。本文只记录本仓库本轮仍有效的实现、验证和交付状态；历史变化见 [`../CHANGELOG.md`](../CHANGELOG.md)。

## 当前目标

本轮将资料库能力并入题库主工作区，并收敛题库、导出和 Agent 的关键交互：题卡、PDF 与一般 Markdown 可按三个独立开关任意组合显示；文件在题库内预览、编辑、分栏和管理；原卷上传、导出选项、批量操作与 Agent 输入区改为更紧凑的按需展开布局。题库格式、讲义轻量接口、导出默认语义、用户数据和固定安装器身份保持不变；本轮仅同步日常目录版，不构建 Setup、不改版本或创建 tag。

## 已落地实现

- 全站单选下拉保留原生 `select` 为表单值源，并增强为圆角 listbox，支持说明、搜索、键盘、焦点恢复、表单 reset 和动态选项。题库、导入、讲义、编辑、设置与 Agent 按“主流程常驻、高级设置折叠”重排，窄屏回落单列。
- 文件树已拆分“子节点已加载”和“当前已展开”，只强制展开活动题集祖先，手动状态按题库保存在当前窗口 `sessionStorage`。题目选择统一兼容布尔值与旧 `0/1`，局部替换、编辑回写和无限滚动均从服务端状态恢复。
- 题卡原地编辑、新题草稿和独立编辑页共用内容驱动的解析／备注折叠语义；有内容默认展开，空内容默认折叠，textarea 在 84–280px 内增高，预览勾选仅属于当前编辑会话。
- Agent 已改为无头像并排工作区，支持安全 Markdown／KaTeX、SSE 增量消息、可展开工具状态、幂等取消和部分回复保留。Chat Completions 与 Responses API 的流式文本和工具参数统一聚合，不完整终态不得执行工具，终态只落盘一次。
- 标准模式继续逐次审批本机命令、题库写入、付费转换和模板操作。危险模式需在当前会话显式确认警告后临时授权，可免确认执行具有当前 Windows 用户权限的本机命令及受控操作；切换会话或应用重启即失效，硬校验、操作锁和审计不受影响。
- 侧栏新增提示词库，两份官方提示词只读，本地自定义提示词支持完整 CRUD 和原子保存。导入支持直接粘贴 Markdown、逐题粘贴原图，以及带清单、入口 Markdown 和 `assets/` 的安全 ZIP；新资产按摘要去重，失败回滚后进入既有逐题校对。
- 设置页新增模板管理。目录 schema 已升级到 v2；可执行模板接受 UTF-8 TeX 或清单 ZIP，PDF 只作参考。模板必须通过 `$body$`／运行时宏／资源与危险原语检查，并在受限 TeX 工作区按全部声明模式真实编译、绑定源码哈希和确认预览后才可启用；导出继续以内置模板兜底。
- `2026-08-31` 续修选择题选项布局：服务端为选项标签预留宽度，前端按当前容器实际尺寸在 4／2／1 列间复核并在宽度变化后恢复首选列数；题卡节点替换时同步解除旧尺寸监听。PDF 导出同时保护 `\text`、`\textbf`、`\textit`、`\hbox` 等文本参数，避免“其中”被改写为文本模式中的 `\mathrm{M}`，并修复普通文本带圈数字显示为命令源码。
- `2026-09-01` 将导出面板和行内编辑器改为按自身容器宽度降级：导出首行按 6／3／1 列、高级设置与编辑元数据按 4／2／1 列、页眉页脚和编辑工作区在窄容器中转为单列；所有输入和网格子项允许安全收缩，导出或编辑期间隐藏新增题目悬浮按钮。原卷面板也按容器宽度换行，长文件名不再被操作区压成逐字竖排。
- 已选题目改为右缘边签和原生模态抽屉，主内容宽度不再随选题数量变化。抽屉使用独立只读题卡，完整展示题型、难度、星标、题源、目录、题干及折叠的解析、备注和标签；详情按需加载并用 generation 丢弃迟到响应。`/api/selection` 增加权威数量和只读详情，`/api/handouts/selected` 及全部写接口保持不变。点击题干始终创建新标签并按原目录跨 30 题分页定位，连续点击、切换标签、关闭抽屉或目标失效都会取消旧任务。
- 图片重绘现在为每道题的每张图片保留原图和 AI 生成版本历史，记录模型、时间与附加要求；题卡可查看缩略图、切换版本并删除不再使用的生成版本，原图和共享资源受到保护。
- README 中英文首页引用的题库、批量导入和讲义三张截图已用完全隔离的演示题库重拍为 `1440×960`，标题栏、导航和工作区均为 `v2.0.0`，不含真实题库、路径、账号、个人 Provider 配置或凭据。

## 发布验证

- `2026-08-31` 最终执行 `tools/verify_release.ps1 -SkipBundleScan -ExpectedTag v2.0.0`，版本门、Python 全量 `unittest` 881/881、Node 全量测试 60/60、28 个 Jinja 模板、36 个 JavaScript 文件语法检查和两个前端 bundle 构建全部通过；目录版扫描在后续 `DirectBundle` 阶段单独执行。
- `2026-09-01` 定向 JSDOM 回归 17/17、`npm.cmd run test:handouts` 73/73、Python 全量 `unittest` 885/885 通过；`app.py`、`handouts.py` 语法检查和全部 Jinja 模板编译通过。`tools\verify_release.ps1 -SkipBundleScan` 随后完整通过版本门、相同的全量测试、模板与 JavaScript 语法检查及两个前端 bundle 构建，目录版扫描在 `DirectBundle` 阶段单独执行。
- 真实 Edge 覆盖 `1612×900` 浅色桌面宿主、`1440×920` 深色桌面宿主、`1024×680` 浅色嵌入模式和 `1024×680` 深色桌面宿主最大题集栏：导出首行分别为 6／3／3／1 列，高级设置为 4／2／2／1 列，极窄页眉页脚、编辑工作区和元数据均为单列。所有检查场景均无容器横向溢出或子项越界，边签不覆盖交互控件，抽屉开关前后主内容宽度不变；极窄原卷长文件名、导出区和行内编辑已保存截图复核。
- 本轮功能验收期间，源码服务 Playwright 41/41 通过，覆盖 1280／1024／640／480 宽度及深浅主题；真实 XeLaTeX 验证覆盖 8/8 导出模式，16/16 次 Pandoc／XeLaTeX 命令全部通过。
- `2026-08-31` 续修后重新执行 `DirectBundle`，目录版扫描 1,377 个文件，受保护数据摘要前后完全一致，安装版以 `2.0.0`／`2.0.0.0` 健康启动；随后用真实选题篮的同一批 19 道题导出 119,744 字节 PDF，响应类型为 `application/pdf` 且文件头为 `%PDF-`。
- `2026-09-01` 首次覆盖后的最终保护检查准确捕获到安装版窗口同时产生的真实选题写入（`/clear -> /select_all -> /clear`），因此没有把变化误报为更新成功，也没有擅自恢复此前 19 道题；当前选题篮按该次界面操作保持为空。关闭运行窗口后复用已验证 bundle 执行第二次 `DirectBundle -SkipBuild`，9/9 步骤全部通过：扫描 1,377 个文件，安装文件与 bundle 哈希一致，凭据、许可证、题库登记、任务、续传及其余受保护数据摘要保持不变，版本为 `2.0.0`／`2.0.0.0`，首次健康启动端口为 `62956`、PID 为 `21916`。
- 随后的独立复核从日常安装目录重新启动 `QuizForge.exe`，`/healthz` 返回 `quizforge/ok`，题库为 `D:\data\笔记本\Obsidian\QuizForge\数学`，当前端口为 `57395`、PID 为 `29032`，进程路径和项目路径均指向 `D:\data\学习\清华\课程资料\大一暑期小学期\AI coding\CLAUDE\QuizForge`。
- `2026-09-02` 图片版本功能的真实页面验收定位到 `2027届深圳中学开学考第16题`（qid `4259750d90b2`）的第 1 张图：历史弹窗显示原图和 `gpt-image-2` 生成版本 `redraw_8a846a8e56769a83.png`，点击“切换”后题卡即时加载生成图（1441×1091），未发生整页刷新。
- `2026-09-04` 最终执行 `update_installed.ps1 -DirectBundle`：Python 编译、全量单元测试 893/893、前端构建与 Node 测试 77/77 全部通过；目录版扫描 1,378 个文件，覆盖后程序文件与 bundle 哈希一致，凭据、许可证、题库登记、任务和续传数据摘要保持不变，`2.0.0`／`2.0.0.0` 在 `127.0.0.1:54346` 健康启动。导出另以 8 种模式 × 3 种解析位置穷举 24 组组合，合成题卡与随机真实备份题卡均为 24/24 成功，导出相关测试 68/68 通过。
- 最终目录版成品扫描检查 1,377 个文件，未发现业务源码、私密数据、私钥或废弃程序文件。已在独立的 `CodexSandboxOffline` 用户与 HKCU 中完成 `1.1.0 -> 2.0.0` 真实 Setup 覆盖：新版未传 `/DIR` 仍复用旧目录，两版健康启动、版本与关于页、用户数据和题库哈希、5 个废弃文件精确清理、未知文件保留、目录版逐文件哈希及官方卸载均通过；日常安装目录和主账户快捷方式前后摘要不变。可信 Authenticode 仍待完成，未执行的检查不得视为通过。

## 发布状态

- 源码、桌面目录版、安装器脚本、Windows 文件版本和前端包版本均已统一为 `2.0.0`／`2.0.0.0`；固定 Inno Setup `AppId`、`UsePreviousAppDir=yes` 和用户数据隔离边界保持不变。
- `v2.0.0` 日常目录版已在最终源码上执行 `DirectBundle` 并健康启动；覆盖前后摘要确认 Agent、提示词、模板、任务、续传状态和凭据等受保护数据一致。更新同时按固定白名单移除 5 个废弃授权／预览程序文件，用户配置和历史兼容数据未进入清理范围。
- `2026-08-31` 曾在当轮两项修复后重新生成 `build/private-test/QuizForge-2.0.0-Private-Test-Unsigned-Setup.exe`，大小 62,189,978 字节，版本资源为 `2.0.0.0`，Setup 与内层主程序均为 `NotSigned`，SHA-256 为 `0BA725D7F8FA8FB94DE282897075ABC1371DFD1AD9D7F703673009D2D99D8456`。固定 `AppId`、`UsePreviousAppDir=yes`、安装／清理脚本和用户数据边界保持不变，覆盖能力沿用上方同配置的 `1.1.0 -> 2.0.0` 隔离验收；该位级候选未再次执行专用账户覆盖，仅由维护者点对点提供给少量受信任测试者，不进入仓库或公开下载渠道。
- `2026-09-01` 本次未重新构建 Setup，现有未签名候选、版本号和公开发布状态均未改变。
- `2026-09-04` 本次仅重建并覆盖日常目录版，未构建 Setup；现有未签名候选、版本号、tag 和公开发布状态均未改变。
- 公开发行必须同时对主程序和 Setup 完成可信 Windows Authenticode 签名。当前 SignPath 仍在审核，本机没有代码签名证书；尚未创建 `v2.0.0` tag、公开 Release 或更新清单，未签名候选不得作为公开资产上传。
