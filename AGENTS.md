# QuizForge 软件版协作规则

@CLAUDE.md

本文件是 `软件版/` 范围内的首要项目规则。`CLAUDE.md` 继续保存架构、安全边界和具体避坑信息；两者冲突时，以用户本轮指令、当前代码和本文件依次优先。

## 1. 产品阶段与工作目标

QuizForge 软件版已经进入持续开源演进阶段，按 `GPL-3.0-or-later` 发布。每次改动既要解决当前问题，也要保护长期可维护性、用户数据、旧版本兼容和后续发布能力，但仍按 L2 小产品管理，不为尚未出现的规模引入公司级流程或过度抽象。

用户主要提供产品思路和目标，不默认承担技术方案设计。执行者应先理解真实目标，结合现有实现比较可行方案，再选择最小、可靠、可验证的实施方式。

## 2. 决策与讨论边界

出现以下高影响不确定项时，必须在实施前向用户说明现状、可选方案、主要权衡和推荐方案，确认后再继续：

- 产品方向、产品定位、功能范围、用户流程或关键交互语义；
- 题库格式、配置格式、安装目录、数据迁移与旧版本兼容；
- 收费、许可证、更新授权、联网服务和第三方付费调用；
- 数据删除、覆盖、密钥轮换、安全边界及其他不可逆操作；
- 会明显增加依赖、安装体积、维护成本或改变模块边界的方案。

普通、可逆、不改变用户语义和兼容边界的技术细节，由执行者查阅代码、比较方案后自主决定，不为每个实现细节打断用户。重要取舍要在代码注释、产品文档、CHANGELOG、ADR 或交付说明中留下适合其生命周期的理由，不把临时推理堆进长期文档。

遇到文档与代码冲突时，不得猜测；先查实际实现和 git 历史，仍无法确定时再与用户讨论。

## 3. 日常修改与安装版同步

运行时业务代码、模板、静态资源或随包资源的默认闭环是：

1. 在 `软件版/` 源码仓库中实施最小变更，不直接手改日常安装目录。
2. 按 `CLAUDE.md` 和改动风险完成语法、自动化测试及必要的真实页面／导出验证。
3. 源码验证通过后，关闭正在运行的 QuizForge，执行 `update_installed.ps1 -DirectBundle`，原位覆盖默认日常安装目录。
4. 依赖更新器核对加密密钥、OCR／LLM 配置、题库登记、任务状态、续传文件以及历史许可证／设备身份等受保护数据；随后检查健康接口及本次相关页面或链路。
5. 验收完成后按项目交付格式说明源码验证、安装版同步、安装包构建三者的实际状态。

`DirectBundle` 会重建桌面目录版并覆盖程序文件，但不会调用 Inno Setup，也不会生成 Setup 安装包。纯规则、设计文档、CHANGELOG 或 README 修改不改变安装程序内容，因此不重建目录版。

如果安装版同步失败，先保留现场并分析根因；不得用直接复制零散文件到安装目录的方式掩盖失败，也不得在未核对用户数据的情况下反复覆盖。

## 4. 正式构建与覆盖安装

默认不构建安装包。只有用户明确提出“构建安装包”“正式发布”或含义等价的要求时，才运行 `build_installer.ps1`、生成 Setup、变更正式版本或创建 git tag；普通功能完成、源码验证通过或 `DirectBundle` 成功都不构成隐含发布授权。

正式构建与发布必须保持并验证：

- Inno Setup 的固定 `AppId` 不变，`UsePreviousAppDir=yes`，新版可直接覆盖旧版而无需先卸载；
- 程序文件允许按版本覆盖，用户数据、题库、凭据、设备身份和许可证不进入覆盖范围；
- 使用包含中文或空格的真实路径验证安装参数，不能只依赖安装器返回码；
- 隔离环境完成旧版到新版的覆盖升级，核对卸载器、快捷方式目标、EXE ProductVersion／FileVersion 和健康启动；
- 发行目录通过私钥、凭据和运行数据扫描，包含 GPL 与第三方许可文本，并在公开发布前完成代码签名和覆盖升级验收。

构建产物不进仓库。版本号、CHANGELOG、提交和 annotated tag 的顺序继续遵循根级 `docs/VERSIONING.md`。

## 5. 产品与更新文档

软件版采用三层面向产品的文档体系：

- `docs/PRODUCT.md`：产品全景、板块定位、核心原则、关键边界与成熟度，只记录长期有效的结构；
- `CHANGELOG.md`：每次用户可感知改动先进入 `[Unreleased]`，为更新公告保留可直接复用的素材；
- `README.md`：产品入口、快速开始和主要能力概览，通过链接引导到前两份文档，不复制长期细节。

`迁移说明.md` 是服务器版向软件版迁移及早期演进的技术历史档案，不再作为主更新日志。架构或核心选型变化仍写根级 ADR；跨会话状态同步根级 `docs/STATUS.md`。

每次用户可感知的功能、交互、兼容性或限制变化必须同步 `CHANGELOG.md`。板块定位、核心原则、边界或成熟度发生变化时同步 `docs/PRODUCT.md`；入口、安装方式或主要能力变化时再同步 README。不要把同一段长说明复制到三处。

## 6. 任务交付

任务结束时使用根级规定的交付格式，并在“验证结果”中明确列出：

- 源码验证：执行了哪些命令和手动验收，哪些未执行；
- 安装版同步：已执行／不适用／失败，安装路径与健康状态；
- 安装包构建：默认“否（用户未要求）”，若执行则给出版本、文件、哈希和覆盖升级结果；
- 文档同步：PRODUCT、CHANGELOG、README、STATUS 及其他长期文档是否更新。

不得把未运行的检查写成已通过，也不得把目录版覆盖称为正式安装包发布。
