# 迁移说明

追加式变更记录：记录每次涉及 `vendor/project_alpha` 重新同步、依赖关系、
发布流程等"架构性"改动的原因与做法。这是变更记录，不是使用文档——日常
使用看 `README.md`。新记录直接加在最下面，不改旧记录。

---

## 2026-08-07：project-alpha 内置化 与 GitHub 首次发布

把 `project-alpha`（PDF/图片 → MinerU OCR → DeepSeek 规范化的转换库）整体
vendor 进本项目，使其成为完全自包含的系统，并打包发布到 GitHub。

### 1. 背景

改动前，`config.py` 里的 `PROJECT_ALPHA` 指向一个外部路径，本项目的
`converter.py` 依赖这个外部目录才能跑 AI 识别导入。这意味着换机器、或者
把本项目单独发布出去时，AI 导入功能会直接失效（外部路径不存在）。

目标：把 project-alpha 的必要代码整体拷进本项目内部（`vendor/` 目录），
让本项目成为一个不依赖任何外部同级目录的独立系统；同时保留"AI 导入需要
用户自己的 MinerU/DeepSeek 密钥，其余功能离线可用"的产品形态不变。

### 2. project-alpha 源的甄别

发现磁盘上同时存在两份 project-alpha：

- `_backups\20260717_014744_post-autonomy\project-alpha\`——**过期**快照。
- `D:\data\ai-coding\project-alpha\`——**当前活跃、最新**的版本。

直接对比 `src/pipeline.py::run_parse()` 与 `src/normalizer.py::normalize()`
的函数签名确认：只有后者带 `only_numbers: list[int] | None = None` 参数，
而本项目 `converter.py` 恰好会传这个参数。如果错拿了备份里的旧版本，
运行时会直接抛 `TypeError: unexpected keyword argument 'only_numbers'`。
因此最终从 `D:\data\ai-coding\project-alpha\` 拷贝，而不是备份目录。

### 3. vendor 了什么，排除了什么

拷进 `vendor/project_alpha/` 的文件：

```
vendor/project_alpha/
  .env.example
  src/
    __init__.py
    config.py
    deepseek_client.py
    exceptions.py
    mineru_client.py
    normalizer.py
    pipeline.py
    validator.py
  templates/
    normalize_prompt.md
```

**明确排除**（原样留在原 project-alpha 目录，不拷贝）：

- `main.py`——project-alpha 自己的 CLI 入口。本项目的 `converter.py` 是直接
  调用 `src.pipeline.run_parse()` 等库函数，从不走 CLI，所以不需要它。
- `.env`——**真实密钥**，绝不能进 git。只拷了 `.env.example`（安全模板）。
- `.claude/`、`.obsidian/`、`output/`、`__pycache__/`——project-alpha 自己的
  工具产物/配置，与本项目无关。
- `README.md`、`CLAUDE.md`、`AGENTS.md`、`移交说明.md`、
  `QuizForge使用手册.pdf`——project-alpha 自己的文档，本项目有自己的文档。
- `templates/exam_template.tex`——project-alpha 原版只有 791 字节，是个极简
  pandoc 模板；本项目自己的 `exam_template.tex`（5731 字节）已经演化出选择题
  排版、图片布局、页眉页脚等 QuizForge 专属宏，明显更完善，**保留本项目自己
  的版本，不覆盖**。

依赖方面：project-alpha 的 `requirements.txt` 提到 `pyyaml`，但 grep 了所有
vendor 进来的 `src/*.py`，没有任何文件 `import yaml`——确认这是历史遗留的
无效依赖，本项目 `requirements.txt` 未做改动（本项目用 `ruamel.yaml` 处理
自己的 frontmatter 需求，够用）。

### 4. 代码改动

- **`config.py`**：`PROJECT_ALPHA` 由外部路径改为
  `BASE_DIR / "vendor" / "project_alpha"`；`PANDOC` / `XELATEX` 改为先用
  `shutil.which()` 探测 PATH，找不到再退回本机曾验证过的硬编码路径（提升在
  其他机器上的可移植性,该硬编码路径大概率仍需用户自行调整）。
- **`converter.py`**：模块docstring改为描述 project-alpha 是 vendor
  进来的内部包，而非外部依赖；`_make_llm_client()` 里的日志文案从
  "project-alpha 默认 DeepSeek" 改为"内置默认 DeepSeek"。其余机制
  （`_ensure_src_on_path()` 往 `sys.path` 塞路径、`_alpha_cwd()` 临时切
  CWD 读取 vendor 内的 `.env`）不变，只是指向的路径变了。
- **`app.py`**：设置页停用识别配置后的 flash 文案同步改为"内置默认
  DeepSeek"。
- **`templates/settings.html`**：三处 "project-alpha 默认/的默认 DeepSeek"
  文案统一改为"内置默认 DeepSeek"。

均已通过临时 smoke test（起 Flask test_client，不配置任何 AI 密钥，验证
`/`、`/question/new`、`/import`、`/dedup`、`/recycle-bin`、`/settings` 六个
路由都返回 200）确认：没有 AI 密钥时应用能正常启动，`load_config()` 抛出
可捕获的 `ConfigError` 而不会让整个进程崩掉。测试脚本用后已删除。

### 5. `.gitignore`

发现 `crypto_utils.py` 的 docstring 一直声称 `data/` "已在 .gitignore 里"，
但项目根本没有 `.gitignore` 文件——这个说法之前是假的。新建了
`.gitignore`，覆盖：

- `data/`（题库数据、加密密钥 `data/.enc_key`、`data/providers.json`，全是
  本机私有内容）
- `vendor/project_alpha/output/`、`vendor/project_alpha/.env`（真实密钥）
- `output/`（导出产物）
- Python 常规忽略项（`__pycache__/`、`.venv/` 等）
- 任意 `.env`（`!.env.example` / `!vendor/project_alpha/.env.example` 例外）
- IDE 文件、`.obsidian/`

### 6. GitHub 发布

只发布 `软件版/` 本身作为独立仓库，不带上外层 CLAUDE 工作区。

1. `git init`，`git status` 确认未跟踪文件里没有 `data/`、`.venv/`、
   `output/`、任何真实 `.env`。
2. `git add -A`，逐项核对 39 个待提交文件，确认无密钥泄露。
3. `git commit -m "Initial commit: QuizForge standalone edition with vendored project_alpha"`
4. 因本机未安装 `gh` CLI（`gh auth status` 报 `CommandNotFoundException`），
   改为让用户在 GitHub 网页端手动建库，拿到 URL 后：
   ```
   git remote add origin https://github.com/lhd23333/QuizForge.git
   git push -u origin master
   ```
5. 推送成功，远程分支 `master` 建立，本地跟踪配置完成。

仓库地址：**https://github.com/lhd23333/QuizForge**

### 7. 文档

新增双语 README：`README.md`（中文）+ `README.en.md`（英文），互相在开头
链接对方。内容覆盖功能列表、快速开始、AI 导入的两种配置方式（设置页 /
`.env`）、安全须知（仅监听 127.0.0.1、无鉴权、单用户、加密密钥不可逆丢失
警告）、目录结构，以及专门一节说明 `vendor/project_alpha` 是内部库而非
外部服务，其 MinerU/DeepSeek 依赖是不可避免的云端调用。

### 8. Vendor 来源记录

新增 `vendor/project_alpha/VENDORED_FROM.md`，记录本次拷贝的上游 commit
（`9033184c898cb9a2fc321bfb02dae690fa13ffff`，2026-07-17），以后重新同步
时先看这个文件判断上游落后了多少。
