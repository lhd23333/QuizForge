# QuizForge 软件版更新记录

本文件记录用户可感知的新增、改进、修复、兼容变化和已知限制，并作为后续更新公告的事实来源。开发期内容先写入 `[Unreleased]`；正式发布时再定版到对应版本，不以源码完成或日常目录版覆盖代替正式发布。

## [Unreleased]

### 改进

- 配图重绘按模型协议自动分流：Qwen-VL 等视觉聊天模型继续生成可编译的 TikZ 矢量图，`gpt-image-*` 模型改用 Images Edit 生成 PNG，并沿用预览、应用和原图还原。
- 配图重绘新增图片版本历史：每次生成会记录模型、时间和附加要求，可在题卡中查看缩略图、切换当前版本或删除不用的生成版本；原图和被其他题目共享的资源会继续保留。
- 已选题目由常驻右栏收拢为工作区右缘的“已选 N 题”边签；点击后在不改变主内容宽度的覆盖式抽屉中按需读取完整只读题卡，并继续提供移动、复制、批量编辑、移出本集、清空选择和删除勾选等批量操作。详情读取失败可重试，选题归零时抽屉自动关闭。
- 点击已选题抽屉中的题干会保留当前题集标签的分页深度和滚动位置，并始终在新的内部标签中按原目录、自定义顺序跨页定位源题；连续定位、切换标签或目标失效时，旧任务不会覆盖当前状态。
- 选择题选项现在为 A–D 标签和间距预留宽度，中等长度选项优先使用两列；KaTeX 渲染后还会按题卡当前真实宽度在 4／2／1 列之间复核，Agent、选题栏或窗口改变宽度时自动重排，不再把长选项挤在四列中换行。
- 题库筛选新增彼此独立的“展示题卡”“显示 PDF 文件”和“展示一般 MD”开关，三者可任意组合；资料文件可直接在题库文件树中打开。题目勾选后批量编辑与更多操作以右缘标签常驻，Agent 工作目录、权限和模型控件移到输入框下方。
- 原卷上传改为折叠面板，支持将 PDF 拖到原卷面板或文件树题集行直接上传；文件树统一通过右键菜单新建、重命名、删除和触发 PDF 增量转换。

### 修复

- 修复左右区域压缩题库主内容时，导出首行、高级设置、页眉页脚和行内编辑器仍按整窗宽度排版，导致预览、导出及编辑控件越界或被遮挡的问题；相关区域现在依据自身容器宽度逐级降列，导出或编辑期间悬浮新增按钮也不再覆盖控件。
- 修复极窄内容区内原卷长文件名被操作区压成逐字竖排的问题；文件名和操作区现在按原卷面板自身宽度分行，长文件名及来源名称可正常断词。
- 修复 PDF 导出把数学公式内 `\text{，其中 }` 等文本参数中的“左／中／右”误改成 `\mathrm{L/M/R}`，继而触发 `\mathrm allowed only in math mode` 的问题；粗体、斜体、强调和盒子等常见文本命令同样保持原文，文本参数外的数学下标转换继续生效。
- 修复普通文本中的带圈数字经过反斜杠清理后在 PDF 中显示为字面量 `\textcircled{...}` 的问题；导出副本改用受保护的行内数学文本命令，不修改题库原文。
- 动态替换题卡后会解除旧选项网格的尺寸监听并监听新节点，避免长期编辑时累积已脱离页面的 DOM。
- 导出入口改为“导出选项”展开按钮，只有面板内的提交按钮执行导出，避免顶部按钮造成误操作。

### 文档

- 仓库中英文首页共用的题库、批量导入和讲义三张截图已按 `v2.0.0` 界面重新拍摄，展示新版深色工作台、双通道导入、圆角设置区和讲义题卡检查器。

## [v2.0.0] - 2026-08-31

### 发布与安全

- `v2.0.0` 标记 QuizForge 从基础题库工具进入“题库工作台 + Agent + 提示词 + 安全模板”的完整产品阶段；现有 Markdown 题库、用户配置、导入导出默认语义和固定安装路径继续兼容，不要求破坏性数据迁移。
- 本次主版本号由用户明确指定，用于标记产品定位与交互体系的阶段跃迁，是现行“仅不兼容变更提升 MAJOR”规则的一次性例外；覆盖安装和已有数据兼容承诺不变。
- 自定义 TeX 模板升级为 schema v2 管理：上传后必须通过清单、唯一 `$body$`、运行时宏、资源白名单和源码哈希校验，并在 `-no-shell-escape`、受限输入输出、禁用 MiKTeX 自动联网安装的独占工作区中完成声明模式的真实编译，验证通过并确认预览后才能启用；PDF 只作为版式参考。
- 日常 `DirectBundle` 将 Agent Provider 配置、会话、偏好、审计日志、用户提示词、Skill 及完整模板目录纳入受保护数据核对，覆盖程序文件前后均验证这些本机状态未被删除或改写。
- 新增公开的代码签名政策、隐私政策和 SignPath 申请清单，明确单人维护角色、人工签名批准、联网边界与签名事故处置。
- 新增只读权限的 GitHub Actions Windows 构建：从固定地址下载并校验 Pandoc 3.9.0.2，完成源码测试、发行扫描和未签名候选构建；候选仅短期保留供 SignPath 审核，不创建公开 Release。
- `v2.0.0` 未签名安装包仅由维护者点对点提供给少量受信任测试者，用于验证真实环境和旧版覆盖安装；Windows 会显示“未知发布者”。可信签名和复验完成前，不上传 GitHub Release 或发布更新清单。

### 新增

- 新增“提示词”栏目：内置“PDF/图片题目转 QuizForge Markdown”和“PDF/TeX 样式转 QuizForge TeX”两份只读官方提示词，支持一键复制、复制为本地自定义提示词，以及自定义提示词的新增、编辑和删除。
- Markdown 导入新增带图包格式：`.qfimport.zip`／`.zip` 以根目录 `quizforge-import.json`、`questions.md` 和 `assets/` 关联本地图片；导入会拒绝路径穿越、符号链接、缺图、重复路径、伪装类型和压缩炸弹，按内容摘要写入共享图片库并在失败时回滚。校对页也可直接向每道题粘贴原图后再确认入库。
- 设置页新增导出模板管理，可上传 UTF-8 `.tex`、带清单的 `.tex.zip` 或只读 PDF 参考，查看验证状态、真实编译预览、兼容导出模式并在确认后启用；旧 TeX/ZIP 保留但需重新验证，旧 PDF 迁移为只读参考。
- Agent 对话新增 SSE 流式协议与幂等停止：回复、工具状态和审批逐事件更新，停止会关闭上游生成、保留部分回复并阻止尚未开始的工具和后续模型轮次；原 JSON 消息接口继续兼容。
- Agent 上传 PDF、DOCX 与 ZIP 现统一先安全暂存，不再自动启动 OCR；启动前必须选择 MinerU/Doc2X、逐题/整篇导入方式和机械/LLM/人工审核规范化方式。会话新增独立“材料目录”和“题库联动目录”，均限制在当前题库根目录内，未选择材料目录时跟随题库目录。
- 新增右侧 QuizForge Agent 工作区：可从全局导航展开并与题库主区域并排使用，支持多会话切换、会话搜索与删除、当前题库／仅聊天范围切换、题库内工作目录选择，以及 PDF／DOCX／ZIP 试卷上传后的后台识别任务跟踪。标准模式下，本机命令、题库写入、付费转换和模板操作逐次审批；危险模式需在当前会话显式确认警告后临时授权，可免确认执行拥有当前 Windows 用户权限的本机命令及受控操作，切换会话或重启应用即失效，且不能绕过参数、ZIP、模板、CSRF 和审计硬校验。
- Agent 会话工作区新增会话列表宽度拖拽、会话拖放排序和逐条删除；排序保存在本机，删除始终二次确认。模型栏在未配置 Provider 时明确显示本地快捷模式，并在配置面板说明当前使用的 OpenAI Chat Completions 兼容 API；常见兼容服务可通过预设快速填充地址和模型名。
- 新增独立 Agent Provider 配置，可保存并切换 OpenAI 兼容原厂或中转站模型；支持 Chat Completions 与 CC Switch 使用的 Responses API，可导入用户主动选择的 CC Switch `config.toml`／`auth.json`，并保存推理强度、服务等级和响应存储设置。新增声明式 Skill 草稿、Skill 文件夹导入和 LaTeX/PDF 模板登记接口。Skill 与模板启用前必须预览确认，模板导出继续复用现有 TeX/PDF 链路并打包自定义资源。
- Agent 新增只读 QuizForge 浏览工具，可查看题库、资料库、回收站、图片附件、讲义和识别历史；导出工具支持显式指定当前题库内的输出目录，未指定时使用会话工作目录。
- Agent 新增受控 `execute_command` 工具，可在会话选定的材料目录、题库联动目录和导出目录内执行 PowerShell、CMD 或 Python；删除、覆盖、移动、重命名及写文件等破坏性命令在标准模式下需要审批，危险模式可直接执行，命令输出进入会话与审计链路。
- 批量导入新增“强制白名单”题号边界：内置识别 `x.`、全角点号、`第x题`、`第 x 题` 和中文数字 `第X题`，自定义题号模板可作为额外白名单；跳号、乱序和重复题号均按原文顺序保留，不再触发自然连续性拦截。
- 多张图片合成 PDF 后使用同轮 MinerU 页面证据隔离原图片页；下一页首个白名单题号前的游离文字不再污染上一题。页界证据缺失、重复或逆序以及 Doc2X 多图任务会保留原文并强制人工校对，不猜测或静默删文。
- 题卡顶部显示可读文件名，“更多”菜单支持直接重命名；题目稳定 ID 与图片引用不随文件名变化，在 Obsidian 中改名后界面也会同步使用新名称。
- 新增按题库隔离的“历史记录”：每组识别默认归档原文件与最终 Markdown，删除先进入独立回收站；归档 Markdown 可直接建立一组待审核任务，不重复调用 OCR 或识别模型。
- 资料库文件树新增“历史记录”入口，可只读查看归档 PDF／图片，并用现有阅读／源码模式编辑 `result.md`；保存继续校验文件版本，修改后的 Markdown 会被“再次提取”直接使用。
- 资料库新增顶层／子文件夹创建和文件／文件夹重命名；已打开标签会随路径原位更新，未保存的 Markdown 草稿不会因文件夹改名丢失。
- 资料库文件与文件夹支持直接拖入目标文件夹：普通拖动为移动，按住 Shift 放下为复制，拖到资料库标题栏或文件树空白处可移回根目录；移动会同步已打开标签并保留 Markdown 草稿，复制题卡 Markdown 时生成独立 ID。
- 资料库开始索引 DOC 与 DOCX：DOCX 不直接打开，只在用户明确选择后加入转 Markdown 或转 PDF 的后台任务；旧版 DOC 需先用 Word／WPS 另存为 DOCX，不会隐式改写原文。
- 资料库新增 PDF 提取、排序、旋转、拆分和合并工具。输入和输出均使用资料库相对路径，合并时每行填写一份资料库 PDF；默认产物保存在源文件夹，分别使用“原名-提取／排序／旋转.pdf”“第一份原名-合并.pdf”和“原名-第x-y页.pdf”，已有同名文件不会被覆盖。
- 资料库新增自然制卡入口：Markdown 可从选区或粘贴文本制卡，PDF 可按页／全文识别，也可直接在当前阅读画面中分别框选题干与解析，PNG／JPG／WEBP／BMP 图片可直接进入识别流程。支持单题／多题以及智能连续性审查／允许跳号的白名单分题，默认写入“临时卡片”。PDF 原位框选仅在 Windows 桌面版可用。
- PDF、DOCX 与资料制卡统一作为可持久化的后台资料库任务执行，并在全局“转换任务”页面独立展示；失败或重启后标记中断的任务可由用户明确重试，不会伪装成需要审核入库的标准批次。
- 题库与资料库顶部支持手动新增空白页；点击题集或资料文件只覆盖当前标签，只有点击右侧加号才会增加标签页。
- 已选题目新增批量题型、难度、标星、题源和备注操作；备注支持追加、覆盖或清空，加入题集时可按层展开完整文件树。
- 已选题目新增“移动到”和“复制到”两种明确操作；复制会创建独立题目并保留题干、解析、图片布局、标签、题源和备注，来源题集保持不变。
- 题卡支持直接拖动：未筛选的叶子题集可在“自定义”排序下调整题序；拖入左侧题集默认移动，按住 Shift 拖入则创建独立副本。
- 题集排序新增“按题源”：相同题源集中显示，题源名称按数字自然排序，同一题源内部沿用原题集顺序，无题源题目统一置后。

### 改进

- 全站单选下拉采用渐进增强的圆角无明显边框控件，保留原生 `select` 作为唯一表单值源；支持说明文字、选中勾、长列表搜索、上下翻转、完整键盘导航、表单重置和动态选项，脚本不可用时自动回退原生控件。
- 题库、导入、讲义、编辑、设置和 Agent 按“主流程常驻、高级设置折叠”重排操作区，统一 16–18px 线性图标并在窄屏转为单列。Agent 对话移除头像和欢迎插画，发送键仅保留向上箭头；助手正文使用安全 Markdown、表格、代码复制与现有 KaTeX 渲染，原始 HTML 和危险链接协议保持关闭。
- 题卡原地编辑、新题草稿和独立编辑页的解析／备注改为内容驱动的折叠区：有内容默认展开，空内容默认折叠，编辑框在 84–280px 内随内容增高并在超限后内部滚动；两个字段可分别控制是否显示在当前编辑预览，该选择不写入题库。
- 建立首期 Codex 风格视觉基线：新用户默认使用柔和石墨深色和中性灰工作区，WIMath 蓝仅用于交互与品牌；桌面外壳、全局导航、设置页和主要工作区统一采用行式布局与线性图标规范。CSS 约定和品牌资产边界分别记录在 [`docs/CSS_STYLE_GUIDE.md`](docs/CSS_STYLE_GUIDE.md) 与 [`docs/BRAND_ASSETS.md`](docs/BRAND_ASSETS.md)。
- Agent 批量追加标签不再限制题目数量和标签数量；仍会校验题目 ID、标签内容、当前工作目录，并在标准模式下要求确认。
- 资料库 PDF／图片制卡新增“大模型规范化”开关并默认关闭；关闭时复用 OCR 后的纯机械拆题链路，不再因未配置 LLM 而失败。保存位置改为已有题集选择器并默认“临时卡片”，另可直接填写单题名称或多题命名前缀。
- 同一题源批量导入的题卡按“题源第x题”命名；没有题源和目标题集的单题、散题统一进入“临时卡片”，按“临时卡x”递增命名，不再把随机 ID 文件散落在题库根目录。
- 题集树移除文件夹名前重复的小图标，文件夹整行空白区与名称均可点击进入；展开箭头和右侧管理按钮继续保持独立操作。
- 资料库文件树明确以名称排序；拖放只负责跨目录移动或 Shift 复制，不把同目录拖动解释为自由排序。
- 题库启动时主区保持空白，不再自动枚举全库路径或读取首批题卡；资料库启动也不恢复上次文件内容，只保留分栏布局。
- 390px 窄屏下资料库改为上方文件树、下方阅读编辑区，源码模式、保存状态和操作按钮不再被侧栏挤压或裁切。
- 日常覆盖更新把全部历史清单与 Markdown 纳入内容哈希校验，并核对归档原文件的路径、大小和修改时间；历史目录不会进入发行包或被程序文件覆盖。
- 新建文件夹改为在左侧树原地插入，不刷新或展开其它分支；题源以较大的灰色文字紧跟星标显示，备注无论是否填写都可像解析一样紧凑展开。
- 题库下滑时顶部题集标签栏保持可见；每个标签继续独立保存搜索、筛选、排序、已加载深度和题卡位置，切换回来恢复到原题卡。
- 导出面板新增“显示题源”选项；启用后在题号与题干之间加入 `【题源】`，默认关闭以保持原有产物不变。
- 极窄桌面窗口缩减题集树占比，并允许排序和原卷上传控件收缩，390px 下题库页面不再横向溢出。
- 已选题目栏改为单列滚动显示完整题干；移动与复制始终可见，其余批量操作按需折叠，极窄窗口展开后仍限制在视口内滚动。

### 修复

- 修复旧版覆盖安装只替换同名文件、却保留废弃授权脚本、旧公钥和预览许可文件的问题；Setup 与日常目录版更新现在只按固定白名单清理这 5 个程序文件，并在启动前确认它们已移除，用户配置和历史兼容数据不在清理范围内。
- 修复带图片选择题的最后一个选项被误判为尾部的问题；MinerU 将 D 选项写成换行后接图片哨兵时，四图配选项现在能够完整恢复。
- 导出选择题时移除题干末尾仅用于答题的空括号，避免 `（ ）` 随题干进入试卷。
- 修复题目解析中的带圈数字触发 XeLaTeX `Missing $ inserted`：数学区改用文本模式包裹，普通文本不再把 `\\textcircled` 错误放进 `$...$`。
- 修复窄屏浏览器或 Obsidian 页面中已选题栏固定覆盖题卡编辑器、悬浮新增按钮遮挡编辑控件的问题；已选题栏在非桌面宿主回到文档流，进入题卡编辑时隐藏悬浮按钮。
- 修复运行中的 QuizForge 或并行检查占用源码 `__pycache__` 时可能中断完整验证或 `DirectBundle` 的问题；验证脚本和更新器改用各自独立的临时 Python 缓存目录。
- 修复文件树已加载子节点与视觉展开状态混用导致目录无故展开的问题；首次只展开当前题集祖先，手动状态按题库和窗口保存在 `sessionStorage`，局部换树和按需加载后继续恢复。
- 修复勾选题目已进入右侧选题栏、原题卡却丢失勾选样式的问题；现在统一兼容 JSON `true/false` 与旧 `0/1`，并在局部加载、无限滚动和编辑回写后同步 checkbox、题卡样式与计数。
- 修复 Agent 断流或取消后可能把半成品工具参数当成完整调用的问题；仅完整成功终态且参数 JSON 校验通过的工具才会执行，终态只落盘一次并释放会话操作锁。
- 修复 Agent 在包含 1,491 个目录的真实题库中打开工作目录下拉时冻结的问题：`MutationObserver` 不再按每个新增 `option` 重复重建，而是按每个观察批次对受影响的 `select` 去重并只同步一次；500 个动态选项的回归验证只触发一次重建。
- 修复 PDF 当前画面框选时右侧制卡栏仍遮挡 PDF，以及切换“按页识别／框选当前 PDF”后两组控件同时显示的问题；开始框选后制卡栏立即隐藏，完成或取消时恢复。
- 资料库通用文件操作禁止修改 `_assets`、`_handouts`、`_backups` 及其内容；文件重命名强制保留原扩展名，避免把改名误当格式转换。
- 修复桌面窗口后台线程跨线程读取 WinForms 属性时可能与 GUI 线程互相等待，导致窗口、本地接口同时无响应并被 Windows 关闭的问题；窗口句柄改由 GUI 线程缓存，后台最小化改发异步系统消息。
- 修复“已选题目”中加入题集的目录树被限制在右侧窄栏、挤压下方批量属性的问题；目录树改为锚定选择按钮的覆盖浮层，按视口自动调整宽高和上下位置，深层长目录名可完整换行显示。
- 修复选题篮导出在冷启动时先解析整座题库的问题；现在按已选题目 ID 定向读取并保留原排序，真实 29,619 题题库中 21 道题的收集耗时由约 95.9 秒降至约 0.03 秒。
- 修复 OCR 产生的连续下划线填空标记在数学区触发 XeLaTeX `Missing { inserted` 的问题；普通文本和数学区中的填空线均转换为合法横线，正常下标不受影响。
- 修复勾选或取消题目后题卡跳到列表顶部，以及文件夹改名、移动、删除后整页重载或失去当前题库视图的问题；普通目录操作现在只更新对应树节点，删除当前目录时回退到有效父级或全部题目。
- 修复快速勾选后立即“清空选择”时，异步单题请求可能让蓝色勾选短暂残留或重新出现的问题；勾选、全选和清空现在严格按操作顺序落盘，清空会立即同步所有可见题卡。
- 修复批量移动题目会遗漏来源题集、批量复制会复用原题身份的问题；两种操作均按原题顺序追加，复制使用新的独立 ID。
- 修复无边框窗口最大化覆盖 Windows 任务栏的问题；最大化边界改用当前显示器工作区，并保留系统任务栏窗口、最小化和最大化属性。

## [v1.0.0] - 2026-08-18

### 更新保护

- 更新流程集中保护当前配置及历史兼容数据，包括旧账号缓存、设备身份、许可证、激活文件、Doc2X 配置、MinerU Token、模型配置、界面设置和任务状态；安装覆盖前后及启动健康检查后均做 SHA-256 校验，发现任何变化立即中止。

### 产品转型

- 软件版转为 `GPL-3.0-or-later` 开源、本地优先产品：完全取消账号、设备授权、试用、签到、额度和云端 OCR/TeX 运行路径，核心功能全部免费开放。旧激活、设备身份、许可证和账号缓存继续原位保留，但新版不再读取或校验。
- MinerU、Doc2X 和 LLM 改为全部由用户在本机配置；MinerU/Doc2X 支持多份凭据按忙闲和轮转使用。TeX 只提供本机编译、`.tex.zip` 到 Overleaf 和 HTML/KaTeX 近似预览。
- 服务器只保留可选更新清单服务；设置与关于页提供 GitHub 仓库和 Releases 手动下载入口。清单请求只发送版本和平台信息，要求 HTTPS 和结构校验，不自动执行下载内容。
- 左下角保留条件式赞助入口，赞助地址由 `QUIZFORGE_SPONSOR_URL` 配置，未配置时不显示。
- 新增 GPL 正式许可、贡献指南、安全策略、社区行为准则、Issue 模板和 Pull Request 模板。

### 新增

- 每道题新增可折叠备注，支持 Markdown/KaTeX、题卡原地编辑、普通文本搜索和 `note:` 结构化搜索；备注仅用于内部整理，默认不进入导出产物。
- 每张题卡新增单题 TeX ZIP 导出，可直接打包当前题目的题干、图片和解析并上传到 Overleaf。
- 合集机械分组失败后可进入原文件与 OCR Markdown 对照页，人工调整 Markdown 后选择整份识别或按标题重新分组；重试复用既有缓存，不再次调用 OCR。
- 讲义导出新增 Markdown 格式，保留题号、正文、公式、图片引用和解析，同时移除 QuizForge frontmatter、内部块标记和 TeX 版式指令，可直接交给其他 Markdown 工具继续编辑。
- 设置页加入 MiKTeX 一键安装安全门控；实测官方 25.12 Basic Installer 与 Setup Utility 均无有效 Authenticode 签名，当前入口安全关闭，不下载或执行未签名程序。
- 题库搜索新增结构化条件：支持 `tag:`、`content:`、`solution:`、`source:`、`type:`、`difficulty:` 和 `starred:`，多个条件可组合使用，带空格的值可用双引号包裹。
- 题库首页组卷新增 Word 文档导出：普通试卷、标准试卷、双栏刷题、横向课堂演示等现有模式均可生成可继续编辑的 DOCX，公式、表格、图片、题号、解析、分页和页眉页脚保留为 Word 原生结构。

### 改进

- 识别模型新增 DeepSeek 与阿里云百炼（Qwen）预设，可选择常用模型或继续手填自定义模型；界面明确区分模型上下文窗口与可配置的最大输出，并按模型给出推荐输出值。
- 联网更新支持用户确认后自动下载、SHA-256 与 Authenticode 双重校验、静默覆盖和重启；服务器尚未发布 Windows 版本时明确显示“尚未发布可用更新”。
- 全局界面改为固定左侧导航，窄屏自动收为图标轨道；题库、讲义、资料库、导入、任务、查重、回收站、设置和关于页统一为更克制的工作台样式。
- 设置页按“本地、OCR、导出、模型、外观”分区，一次只显示当前分类；各页面左下角新增帮助入口，详细说明统一收进按页切换的右侧抽屉。
- 题库导航改为分层文件树，顶层目录直接显示一级子目录，更深层级按需展开；题库文件树、已选题栏、讲义编排栏、题卡属性栏和资料库侧栏均可拖动调整宽度，并记住用户设置。
- 查重改为手动开始扫描，扫描过程中可暂停和继续，进入页面不再自动遍历整个题库。
- 搜索固定作用于当前显示的完整题目范围：与当前文件夹、标签、题型、难度和星标筛选取交集，同时覆盖尚未滚动加载的题目。
- 普通文本搜索补充题源内容；搜索、切换文件夹或筛选时保留当前范围，“清除”只移除搜索条件。
- 日常 `DirectBundle` 允许在正式版本号不变时覆盖开发安装版；更旧版本和正式安装器路径仍保留版本递增保护。

### 修复

- 修复 HTML/KaTeX 近似预览返回未登记取件号、弹窗第二跳显示 404 的问题；HTML 产物同时以正确的网页类型内联加载。
- 修复“关于”页位于桌面常驻 iframe 时取不到父窗口原生接口，导致打开题库、日志和运行数据目录三个按钮全部报错的问题；更新按钮也统一通过同一桌面桥调用。
- 修复题目浏览快照过期后只显示“点击重试”文字、却没有可见操作入口的问题；现在会显示明确的“重新加载”按钮，并保留当前文件夹、搜索、筛选和排序重新建立快照。
- 修复合集 OCR 原文调整页在 390px 窄屏下底部操作栏覆盖 Markdown 编辑框的问题；窄屏改为上下布局和文档流操作栏，桌面端继续支持拖动分隔条并双击复位。
- 修复 390px 窄屏下顶部导航和桌面标题栏横向溢出，设置页与管理后台的操作控件不再越出视口。
- 修复题库首页只显示空白工作区、不加载首批题卡的问题；现在打开题库后直接显示首批 30 道题。
- 修复从讲义等栏目切回题库时重复重建题卡、出现明显停顿并丢失无限滚动进度的问题；题库改为独立保活，回切后继续沿用原题卡数量、滚动位置和分页快照。
- 修复讲义“已选试题”冷启动时解析整座题库、等待时间过长的问题；热启动直接复用题卡缓存，冷启动只读取 frontmatter 头部定位已选 ID，再完整解析命中的少量题目，同时复用正式题卡渲染显示公式、选项和图片布局。
- 修复解析图文混排会把图片固定放到整段解析右上角的问题；图片前内容保持整宽，从图片引用所在行才开始绕图，超过图片高度后恢复整行宽度，网页预览与 TeX/PDF 导出保持一致。
- 修复中窄窗口下业务 iframe 重复扣除导航宽度，以及讲义属性栏、已选题栏和操作控件相互挤压的问题；极窄窗口下右侧栏改为抽屉。
- 修正双栏 DOCX 中宽表可能导致 Word 长时间重排，以及连续分节产生异常空白标题页的问题。
- 修复 Windows 杀毒软件短暂扫描刚生成的 DOCX 时可能导致原子替换失败的问题；仅对临时文件锁做短时有界重试，持续占用仍会明确报错。
- 修正文末解析样式标记被当作普通文字写入 DOCX 的问题。

### 移除

- 设置页移除账号、短信、授权和云 TeX 控件；旧 `.qflicense`、`activation.json`、`device_identity.dat`、`doc2x.json`、`cloud_account.json` 等兼容文件继续原位保留。

### 兼容与迁移

- Word 导出沿用随包 Pandoc，不新增 Word COM、LibreOffice 或云端转换依赖；Word 导入仍按现有“先转 PDF，再识别”流程，不迁移用户数据。
- `license.qflicense`、`doc2x.json` 与 `cloud_account.json` 均属于升级保护文件；日常目录版覆盖不得修改、删除或用发行包内容替换。
- MiKTeX 一键安装仅在固定来源具备有效 Authenticode 时开放；当前官方安装源未签名，用户可自行安装可信来源的 MiKTeX/TeX Live，或导出 `.tex.zip` 到 Overleaf、使用 HTML/KaTeX 近似预览。

### 已知限制

- 结构化搜索当前统一按 AND 组合，尚不支持括号、OR、NOT、通配符和模糊匹配。
- 未建立搜索索引前，首次对上万题的“全部题目”范围搜索仍需扫描 Markdown，可能明显慢于单卷或小文件夹搜索。
- DOCX 以可编辑内容和语义结构为目标，不与 PDF 逐像素一致；Word／WPS 的字体、换行、换页和换栏可能自然重排。纸张底色与 WIMath 标志仍只用于 PDF／TeX，独立讲义工作区暂不提供 DOCX 导出。

## [0.17.0-beta] - 2026-08-15

### 新增

- 增加全局共享图片目录：全部登记题库可共用一个由 `desktop.json.assets_dir` 指定的图片库。
- 查重页增加跨题库共享图片体检，可识别未被题目、回收站、讲义或安全备份引用的孤儿图片。

### 改进

- 切换共享图片目录时，无损合并各题库旧 `_assets`：同内容文件复用，同名异内容拒绝覆盖，来源文件不删除。
- 题目彻底删除和清空回收站改为跨全部登记题库确认引用，降低误删其他题库共用图片的风险。
- 统一软件版完整验证入口，并拆分导入图片默认规则与导出表格解析的纯逻辑边界。

### 修复

- 修正公共 Obsidian vault 子目录场景下的图片寻址能力，使用户可显式选择公共父 vault 的 `_assets`。

### 兼容与迁移

- 保持固定安装器 AppId 和原安装目录，已验证从 `0.15.3-beta` 原位覆盖升级到 `0.17.0-beta`，卸载器和隔离用户数据保留。
- 日常安装版可通过 `update_installed.ps1 -DirectBundle` 原位覆盖程序文件，无需生成 Setup 安装包。

### 已知限制

- 当前安装包尚未做 Authenticode 签名；联网更新目前只支持手动检查清单，尚未自动下载或安装。
- 安装包内置 Pandoc，可直接导出 TeX 和 ZIP；直接生成 PDF 仍需本机安装 MiKTeX 或 TeX Live。
- 历史题库使用公共父目录图片时，需要用户在题库管理器中显式选择正确的共享 `_assets`，程序不会自动猜测或删除旧目录。

## 更早版本

`0.17.0-beta` 之前的移植过程、功能演进和逐轮验证保留在 [`迁移说明.md`](迁移说明.md)。该文件是技术历史档案，不再作为后续版本的主更新日志。
